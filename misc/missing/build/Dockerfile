# Use the official Nginx image from the Docker Hub
FROM nginx:alpine

# Copy the HTML file to the appropriate directory in the Nginx container
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80
